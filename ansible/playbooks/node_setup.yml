# NOTE: DNS record injection moved to playbooks/dns.yml (runs after the cluster is up).

# - name: Configure node basic things serially
#   hosts: nodes
#   gather_facts: false
#   serial: 1
#   roles:
#     - role: python
#     - role: sudoers

- name: Configure node basic things
  hosts: nodes
  gather_facts: false
  roles:
    - role: node-common

- name: Raspberry pi specific tasks
  hosts: rpi
  gather_facts: true
  roles:
    - role: rpi-common
