dns_key_name: "your-key-name"
dns_key_secret: "your-key-secret"
dns_server_ip: "192.168.1.1"
dns_zone_name: "example.com" # Ensure this matches the inventory variable
dns_record_type: "A" # Ensure this matches the inventory variable
