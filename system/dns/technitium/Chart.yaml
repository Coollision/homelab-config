apiVersion: v2
name: technitium
description: Technitium DNS Server — clustered primary+secondary authoritative DNS + ad-blocking
type: application
version: 0.1.0
appVersion: "15.2.0"

dependencies:
  - name: shared-lib
    version: 0.1.0
    repository: file://../../../lib/shared-lib
