---
# Disable Longhorn replica scheduling on worker-silver VM node
# This prevents high-churn rebuild activity on the unstable VM
apiVersion: longhorn.io/v1beta2
kind: Node
metadata:
  name: worker-silver
  annotations:
    argocd.argoproj.io/sync-options: Validate=false
spec:
  allowScheduling: false
