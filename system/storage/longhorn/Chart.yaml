apiVersion: v2
name: longhorn
description: Longhorn distributed block storage with extras (recurring jobs, storage classes, ingress, volume protection)
type: application
version: 0.1.0
appVersion: "1.11.0"

dependencies:
  - name: longhorn
    version: 1.11.1
    repository: https://charts.longhorn.io
