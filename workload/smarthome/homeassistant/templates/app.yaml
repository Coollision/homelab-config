{{/* Single-app chart since the bundled MariaDB was removed (recorder now on CNPG
     smarthome-db-pg, 2026-07-22). Values are top-level; no per-app multiplexing needed. */}}
{{ include "shared-lib.all" . }}
