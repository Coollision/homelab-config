apiVersion: v2
name: printflow
description: Server-side slicing for PrintStash — retarget a 3MF to a real printer, gates and colours
type: application
version: 0.1.0
appVersion: "0.1.0"

dependencies:
  - name: shared-lib
    version: 0.1.0
    repository: file://../../../lib/shared-lib
