apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ .Values.shared.awsConfigMapName }}
  namespace: {{ include "aws-tunnels.namespace" . }}
data:
  config: |
{{ tpl (.Files.Get "source/aws-config") . | indent 4 }}
